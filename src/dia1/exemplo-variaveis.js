// String (texto)
const mensagem = "Olá, JavaScript!";

// Número
const idade = 25;
const preco = 19.99;

// Booleano (verdadeiro/falso)
const isDeveloper = true;

// Array (lista)
const frutas = ["maçã", "banana", "laranja"];

// Objeto (dados estruturados)
const pessoa = {
  nome: "Joana",
  idade: 30,
  cidade: "São Paulo"
};